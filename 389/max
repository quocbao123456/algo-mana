/**
 * @param {string} s
 * @return {number}
 */
var maximumLength = function(s) {
    for (let i = 0; i < s.length; i++) {
        for (let j = i + 1; j < s.length; j++) {
        
        }
    }
};

maximumLength('aaaa')